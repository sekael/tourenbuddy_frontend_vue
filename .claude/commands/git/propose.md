Fetch GitHub issue $ARGUMENTS using `gh issue view $ARGUMENTS` (or `gh issue view <number> --repo <owner>/<repo>` if a URL was provided). Read the issue body and any linked context.
Propose implementation for the GitHub issue according to conventions for specification proposals.
If necessary, grill me until all open questions are resolved.

